/**
 * PageNotFound component for displaying a 404 error message.
 * @returns {JSX.Element} The rendered PageNotFound component.
 */
function PageNotFound() {
  return <div>Sorry, page not found.</div>;
}

export default PageNotFound;
